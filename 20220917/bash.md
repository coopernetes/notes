# bash
Stuff to not forget.

`$?`: exit code of last run command  
`$#`: number of args  
`$0`: script name?  
`$1, $2, ...`: positional args  
`some_command 2>&1`: combine stderr + stdout  
`cd -`: go back to last dir  

